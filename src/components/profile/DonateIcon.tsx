import { BitcoinIcon } from '@/components/icons/BitcoinIcon'
import { BsCurrencyBitcoin, BsEthernet, BsFacebook } from 'react-icons/bs'

export default function DynamicIcon ({ type }: { type: string }) {
  return (<BitcoinIcon />)
}
